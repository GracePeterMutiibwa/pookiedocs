---
title: Deployment
---

# Deployment

Build and ship.

```bash
pookiedocs build
```

The `dist/` folder is a self-contained static site. Drop it on Cloudflare Pages, Netlify, a VPS, or wrap it in a Docker container.
